# DuelPlay v13

- Home hero title moved slightly higher; subtitle closer to main slogan.
- Home CTA row and stats moved slightly lower.
- Create Match hero copy moved up/right on desktop.
- Create Match settings panel no longer receives seasonal artwork as its panel background.
- Season admin start/end date fields split into native date + time controls so the year is always a clean 4-digit date segment and cannot visually merge with the time.
- Existing seasonal background/particle behavior preserved.
