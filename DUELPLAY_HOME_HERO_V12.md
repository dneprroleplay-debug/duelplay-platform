# DuelPlay Home Hero v12

- Main hero slogan is now a single centered line on desktop.
- Supporting sentence is centered directly below it.
- Real Create Duel / Open Duels buttons moved to the lower center in one row.
- Active duels / format / commission cards are directly below the two buttons.
- Hero LIVE LOBBY card removed from the artwork area; live matches remain available in the dedicated Live section below.
- Seasonal hero artwork remains automatic through the global season system.
- Create Match artwork now uses the same automatic seasonal hero artwork via `.create-season-art`.
