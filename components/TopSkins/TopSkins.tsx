"use client";
import {useEffect,useState} from "react";
export default function TopSkins(){const[skins,setSkins]=useState<any[]>([]);useEffect(()=>{fetch("/api/top-skins",{cache:"no-store"}).then(r=>r.json()).then(d=>setSkins(d.skins??[])).catch(()=>{})},[]);if(!skins.length)return null;return <section className="mx-auto max-w-7xl px-4 py-10 sm:px-6"><div className="panel rounded-3xl p-7"><div className="flex items-end justify-between gap-3"><div><span className="pill">TOP SKINS</span><h2 className="mt-3 text-3xl font-black uppercase">Витрина скинов</h2><p className="mt-2 text-sm text-zinc-500">Избранные предметы сообщества. В дальнейшем этот каталог станет основой для Steam-инвентаря и обмена.</p></div></div><div className="mt-6 grid grid-cols-2 gap-3 sm:grid-cols-4 lg:grid-cols-6">{skins.slice(0,12).map(s=><div key={s.id} className="overflow-hidden rounded-2xl border border-white/7 bg-white/[.025]"><img src={s.imageData} alt={s.name} className="aspect-[4/3] w-full object-cover"/><div className="p-3 text-sm font-bold truncate">{s.name}</div></div>)}</div></div></section>}
