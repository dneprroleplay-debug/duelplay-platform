const API=(process.env.DUELPLAY_API_URL||"").replace(/\/$/,"");const SECRET=process.env.DUELPLAY_JOB_SECRET;if(!API||!SECRET)throw new Error("DUELPLAY_API_URL and DUELPLAY_JOB_SECRET are required");const r=await fetch(`${API}/api/jobs/match-watchdog`,{method:"POST",headers:{"x-duelplay-job-secret":SECRET,"content-type":"application/json"}});console.log(await r.text());if(!r.ok)process.exit(1);
