import fs from 'node:fs';import path from 'node:path';import ts from 'typescript';
const root=process.cwd();let bad=0;function walk(dir){for(const n of fs.readdirSync(dir)){if(['node_modules','.next','.git'].includes(n))continue;const p=path.join(dir,n),st=fs.statSync(p);if(st.isDirectory())walk(p);else if(/\.(ts|tsx)$/.test(n)){const sf=ts.createSourceFile(p,fs.readFileSync(p,'utf8'),ts.ScriptTarget.Latest,true,n.endsWith('.tsx')?ts.ScriptKind.TSX:ts.ScriptKind.TS);if(sf.parseDiagnostics.length){bad++;console.error(p,sf.parseDiagnostics.map(x=>ts.flattenDiagnosticMessageText(x.messageText,' ')).join('; '));}}}}walk(root);if(bad)process.exit(1);console.log('DuelPlay syntax QA: PASS');
