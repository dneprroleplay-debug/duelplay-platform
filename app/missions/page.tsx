"use client";import{useEffect,useState}from"react";export default function Missions(){const[d,setD]=useState<any[]>([]);useEffect(()=>{fetch('/api/missions',{cache:'no-store'}).then(r=>r.json()).then(setD)},[]);return <main className="mx-auto min-h-screen max-w-5xl px-4 pb-20 pt-28"><section className="panel rounded-3xl p-7 sm:p-10"><span className="pill">DAILY MISSIONS</span><h1 className="mt-4 text-4xl font-black">Daily Missions</h1><div className="mt-8 space-y-3">{d.map(x=><div key={x.id} className="rounded-2xl border border-white/5 p-5"><div className="flex justify-between"><b>{x.mission?.name||x.name}</b><span>{x.status}</span></div><div className="mt-2 text-sm text-zinc-500">Progress {x.progress??0} / {x.mission?.target??x.target??0}</div></div>)}</div></section></main>}
