"use client";import {useEffect,useState} from 'react';export default function SeasonsPage(){const[d,setD]=useState<any>();useEffect(()=>{fetch('/api/seasons',{cache:'no-store'}).then(r=>r.json()).then(setD)},[]);if(!d)return <main className="pt-28 text-center text-zinc-500">Loading…</main>;return <main className="mx-auto min-h-screen max-w-6xl px-4 pb-20 pt-28"><section className="panel rounded-3xl p-7 sm:p-10"><span className="pill">SEASON ENGINE</span><h1 className="mt-4 text-4xl font-black">DuelPlay Seasons</h1>{d.active&&<div className="mt-6 rounded-2xl border border-pink-400/20 bg-pink-400/5 p-5"><b className="text-2xl">{d.active.name}</b><p className="mt-2 text-zinc-500">{d.active.theme} · {d.active.mode}</p></div>}<div className="mt-8 grid gap-3 sm:grid-cols-2">{d.all.map((x:any)=><article key={x.id} className="rounded-2xl border border-white/5 p-5"><b>{x.name}</b><div className="mt-2 text-xs text-zinc-600">{x.theme} · {x.mode} · {x.active?'ACTIVE':'scheduled'}</div></article>)}</div></section></main>}
