"use client";import{useEffect,useState}from"react";export default function Creator(){const[d,setD]=useState<any>();async function tournament(){const r=await fetch("/api/tournaments",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name:"Creator Cup",maxPlayers:32,entryFee:0,prizePool:0,startsAt:new Date(Date.now()+3600000).toISOString()})});alert(r.ok?"Tournament created":(await r.json()).error||"Error")}async function request(){const amount=Number(prompt("Payout amount")||0);if(amount>0){const r=await fetch("/api/creator",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({action:"requestPayout",amount})});alert(r.ok?"Payout requested":(await r.json()).error||"Error")}}useEffect(()=>{fetch('/api/creator',{cache:'no-store'}).then(r=>r.json()).then(setD)},[]);return <main className="mx-auto min-h-screen max-w-5xl px-4 pb-20 pt-28"><section className="panel rounded-3xl p-7 sm:p-10"><span className="pill">CREATOR HUB</span><h1 className="mt-4 text-4xl font-black">Creator / Streamer</h1><p className="mt-3 text-zinc-500">Profile, referral, commission and payout history.</p><button onClick={tournament} className="ml-2 rounded-xl border border-white/10 px-5 py-3 font-black">Create tournament</button><button onClick={request} className="mt-6 rounded-xl bg-pink-400 px-5 py-3 font-black text-black">Request payout</button><pre className="mt-8 overflow-auto rounded-2xl bg-black/30 p-5 text-xs text-zinc-400">{JSON.stringify(d,null,2)}</pre></section></main>}
