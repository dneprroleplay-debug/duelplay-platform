import { NextResponse } from "next/server";
import { prisma } from "@/lib/prisma";
export async function GET(){try{const users=await prisma.$queryRaw<Array<{nickname:string;reputation:any;level:number;xp:number;avatarUrl:string|null}>>`SELECT nickname,reputation,level,xp,"avatarUrl" FROM "User" WHERE status='ACTIVE' AND "deletedAt" IS NULL AND "isTestAccount"=false ORDER BY reputation DESC,level DESC,xp DESC LIMIT 50`;return NextResponse.json(users.map(u=>({...u,reputation:Number(u.reputation),level:Number(u.level),xp:Number(u.xp)})))}catch(e){console.error(e);return NextResponse.json({error:"Не удалось загрузить рейтинг"},{status:500})}}
